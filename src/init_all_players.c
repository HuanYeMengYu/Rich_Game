# include "utils.h"

void init_all_players(int init_money){
    init_player(QIANFUREN_NUMBER, init_money);
    init_player(ATUBO_NUMBER, init_money);
    init_player(SUNXIAOMEI_NUMBER, init_money);
    init_player(JINBEIBEI_NUMBER, init_money);
}
